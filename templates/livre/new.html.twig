{% extends 'base.html.twig' %}

{% block title %}Ajouter un livre{% endblock %}

{% block body %}
<h1>Ajouter un nouveau livre</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Enregistrer</button>
{{ form_end(form) }}

<p><a href="{{ path('app_livre_index') }}">Retour à la liste</a></p>
{% endblock %}